Implemented all required features in this test task using swagger, including pagination, sorting, and data validation

quick tips for using sorting feature

1 - attribute field supports folowing option: name, color, tail_length, weight
2 - for order firld its: asc - for ascending, desc - for descending

!!!
Please note that the project might not run properly on another local machine
When committing the project to GitHub, I received a warning that some files might not be included in the repository
Because of that, there’s a chance that certain dependencies or configuration files are missing, which could prevent the application from starting correctly
!!!
If this happens, I would be happy to demonstrate the working project locally via a video call, to show how everything functions in my development environment
!!!
